# Database Configuration
# -----------------------
# SQLite (local development - default):
DATABASE_URL=sqlite:///./data/ke_workbench.db

# PostgreSQL (production - Railway):
# Railway auto-injects DATABASE_URL when PostgreSQL addon is attached.
# Format: postgresql://user:password@host:port/database
# Example: DATABASE_URL=postgresql://postgres:secret@localhost:5432/droit
#
# Note: Railway uses postgres:// prefix but the app auto-converts to postgresql://

# Feature flags
ENABLE_VECTOR_SEARCH=true
DEBUG=false

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS - Comma-separated list of allowed origins
# Use "*" to allow all origins (not recommended for production)
# Production: Add your Streamlit Cloud URL
CORS_ORIGINS=http://localhost:8501,https://pazooki.streamlit.app

# Streamlit
STREAMLIT_SERVER_PORT=8501
